import QtQuick
import Quickshell

import qs

Rectangle {

  height: Theme.barHeight;
  radius: this.height / 2;

  border.width: 1;
  border.color: Theme.current.crust;

  color: Theme.current.base;
}